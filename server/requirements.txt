# Runtime dependencies for the mind-probe Session Server.
# Mirrors [project.dependencies] in pyproject.toml for plain-pip installs:
#   pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
sqlalchemy>=2.0
python-multipart>=0.0.9
websockets>=12.0

# Dev / test (also in [project.optional-dependencies].dev):
pytest>=8.0
httpx>=0.27
