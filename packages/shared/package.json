{
  "name": "@mind-probe/shared",
  "version": "0.1.0",
  "description": "Shared TypeScript types for mind-probe (event envelope + clip manifest). Mirrors server/app/events.py.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
